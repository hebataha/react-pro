import React from 'react';
import PropTypes from 'prop-types';

const Send = props => {
    return (
        <div>
            
        </div>
    );
};

Send.propTypes = {
    
};

export default Send;